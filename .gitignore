node_modules/
dist/
build/
.turbo/
coverage/

# Prisma client is generated from schema.prisma on install and on build.
# Committing it would mean reviewing thousands of generated lines and would
# go stale the moment the schema changes.
apps/api/src/generated/

# SvelteKit's generated route types and manifest, regenerated by
# `svelte-kit sync` on install and on build.
.svelte-kit/

# Environment — never committed (ARCHITECTURE.md §19.4)
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
.idea/
*.swp

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Test artifacts
test-results/
playwright-report/
