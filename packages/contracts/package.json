{
  "name": "@gemone/contracts",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"@gemone/contracts: type declarations only, no tests\"",
    "test:unit": "echo \"@gemone/contracts: type declarations only, no tests\"",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@gemone/tsconfig": "workspace:*",
    "typescript": "^5.9.3"
  }
}
